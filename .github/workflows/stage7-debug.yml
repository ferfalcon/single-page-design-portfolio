name: Stage 7 runner diagnostic

on:
  push:
    branches:
      - workflow/stage-7-plan

permissions:
  contents: write

jobs:
  diagnose:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - name: Check out branch
        uses: actions/checkout@v4
        with:
          ref: workflow/stage-7-plan
          fetch-depth: 0

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '24'

      - name: Diagnose Stage 7 transition
        shell: bash
        run: |
          set +e
          LOG=/tmp/stage7-debug.log
          {
            echo '=== context ==='
            node docs/implementation-workflow/cli/design-workflow.mjs context --json
            echo "context_exit=$?"
            echo '=== advance ==='
            node docs/implementation-workflow/cli/design-workflow.mjs stage advance
            echo "advance_exit=$?"
            echo '=== reopen ==='
            node docs/implementation-workflow/cli/design-workflow.mjs artifact reopen ART-IMPLEMENTATION-BRIEF --evidence "Stage 7 adds repository context and the repository-aware implementation plan to the Lite implementation brief."
            echo "reopen_exit=$?"
            echo '=== status after commands ==='
            node docs/implementation-workflow/cli/design-workflow.mjs status --json
            echo "status_exit=$?"
          } > "$LOG" 2>&1
          git reset --hard HEAD
          cp "$LOG" .workflow/stage7-debug.log
          rm -f .github/workflows/stage7-debug.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A .workflow/stage7-debug.log .github/workflows/stage7-debug.yml
          git commit -m "Capture Stage 7 runner diagnostic"
          git push origin HEAD:workflow/stage-7-plan
