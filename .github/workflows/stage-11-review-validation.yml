name: Stage 11 review validation

on:
  pull_request:
    types: [synchronize]
    branches:
      - main

permissions:
  contents: write

jobs:
  final-review-validation:
    if: github.head_ref == 'agent/stage-10-implementation'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout review branch
        uses: actions/checkout@v4
        with:
          ref: agent/stage-10-implementation
          fetch-depth: 0

      - name: Set up pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: pnpm
          cache-dependency-path: frontend/pnpm-lock.yaml

      - name: Confirm frontend still equals implementation output
        run: git diff --quiet edd76540e89eb4e82c82ab567c183eb1b9abf0f4 HEAD -- frontend/

      - name: Install and build
        run: |
          pnpm --dir frontend install --frozen-lockfile
          pnpm --dir frontend build

      - name: Install temporary accessibility tooling
        run: |
          pnpm --dir frontend add --save-dev @playwright/test@latest @axe-core/playwright@latest --lockfile=false
          pnpm --dir frontend exec playwright install --with-deps chromium

      - name: Start production preview
        shell: bash
        run: |
          set -euo pipefail
          pnpm --dir frontend preview --host 127.0.0.1 --port 4321 > /tmp/astro-preview.log 2>&1 &
          for i in {1..30}; do
            curl --fail --silent http://127.0.0.1:4321/ > /dev/null && exit 0
            sleep 1
          done
          cat /tmp/astro-preview.log
          exit 1

      - name: Run final accessibility checks
        shell: bash
        run: |
          mkdir -p frontend/tests
          cat > frontend/tests/stage-11-accessibility.spec.ts <<'TEST'
          import { test, expect } from '@playwright/test';
          import AxeBuilder from '@axe-core/playwright';

          const url = 'http://127.0.0.1:4321/';

          for (const width of [375, 768, 1440]) {
            test(`axe and semantics at ${width}px`, async ({ page }) => {
              await page.setViewportSize({ width, height: 900 });
              await page.goto(url);

              const results = await new AxeBuilder({ page }).analyze();
              const blocking = results.violations.filter((violation) =>
                violation.impact === 'critical' || violation.impact === 'serious'
              );
              expect(blocking, JSON.stringify(blocking, null, 2)).toEqual([]);

              await expect(page.getByRole('main')).toBeVisible();
              await expect(page.getByRole('heading', { level: 1, name: 'Design solutions made easy' })).toHaveCount(1);
              await expect(page.getByRole('heading', { level: 2 })).toHaveCount(3);
              await expect(page.getByRole('button', { name: 'Previous project' })).toBeVisible();
              await expect(page.getByRole('button', { name: 'Next project' })).toBeVisible();
              await expect(page.locator('[data-carousel-slide]')).toHaveCount(5);
              await expect(page.locator('[data-carousel-status]')).toHaveAttribute('aria-live', 'polite');
            });
          }

          test('keyboard and live announcement remain intact', async ({ page }) => {
            await page.setViewportSize({ width: 375, height: 900 });
            await page.goto(url);
            const next = page.getByRole('button', { name: 'Next project' });
            await next.focus();
            await page.keyboard.press('Enter');
            await page.waitForTimeout(230);
            await expect(next).toBeFocused();
            await expect(page.locator('[data-carousel-status]')).toHaveText('Project 4 of 5');
          });
          TEST
          pnpm --dir frontend exec playwright test tests/stage-11-accessibility.spec.ts --reporter=line

      - name: Self-clean validation runner
        shell: bash
        run: |
          set -euo pipefail
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          rm .github/workflows/stage-11-review-validation.yml
          rmdir .github/workflows 2>/dev/null || true
          rmdir .github 2>/dev/null || true
          git add -u .github/workflows/stage-11-review-validation.yml 2>/dev/null || true
          git commit -m 'Complete Stage 11 accessibility review'
          git push origin HEAD:agent/stage-10-implementation
